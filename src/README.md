# src — весь код игры

В Studio: **`ReplicatedStorage → src`**

```
src/
  Server/     Main, PlayerData, BaseMap, Base, Monster, Lab
  Shared/     Config, BaseUtil, MonsterDefs, MonsterDisplay
  Client/     Hud, BaseMarker, MonsterCard, Lab, UiUtil
  Remotes/    создаются в Main при старте
```

Запуск: `ServerScriptService/Init`, `StarterPlayerScripts/Init`
